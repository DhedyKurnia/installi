# 9router on Cloud PaaS (Infrlo / Railway)

Docker image deployment for [9Router](https://github.com/decolua/9router) AI Router Gateway.

## Configuration
- **Port:** Default `20128` (or dynamically mapped via `$PORT`)
- **Host:** `0.0.0.0`
- **Data directory:** `/app/data`
